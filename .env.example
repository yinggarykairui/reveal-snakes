# Copy this to .env for local dev (never commit .env)
# On Railway this is set automatically when you add a Postgres plugin
DATABASE_URL=postgresql://user:password@localhost:5432/reveal_snakes
PORT=3000
